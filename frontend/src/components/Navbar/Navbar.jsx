import "./Navbar.scss";

const Navbar = () => {
    return(
        <nav className="navbar">
            <h1 className="navbar__title">Fitness Tracker</h1>
        </nav>
    );
}

export default Navbar;